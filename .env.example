# API key for the local model server (LM Studio / llama.cpp).
# Required only if authentication is enabled in LM Studio (Developer → Authentication).
# For llama.cpp server this is not needed -- any non-empty string works.
LMSTUDIO_API_KEY=lm-studio
